# Candidate-action adjudication 1

## Control block

- Status: **FINAL — BIND_MINIMAL_CLOSED_EQUILIBRIUM_EFT; DENSITY_MELTING_NO-GO**.
- Binding: `workerRoute: sol-high; phaseId: phase-2; stepId: candidate-action-adjudication; attemptEpoch: 1`.
- Decision: implement the exact local-U(1), same-scalar Higgs branch below; do not implement an external-frequency, bare-Proca, transfer, or fitted-density branch.
- Ensemble: zero-temperature fixed baryon number/density in the source-free gauge-neutral sector, equivalently its one-variable Legendre transform; no scalar chemical source, external gauge current, or reservoir.
- Evidence: the three immutable Phase-1 reports and their cited `gauge-action-brief/derivation.md` §§minimal–vector, `density-transition-brief/derivation.md` §§1,2,4,boundary, and `generator-closure-brief/derivation.md` §§1–3 plus companion `sources.md` files.
- Repository effect: this immutable adjudication only; no code, maintained producer, article, registry, or manifest was changed.

## Bound action and vacuum

Use signature `(+---)`, `Phi=W exp(i theta)/sqrt(2)`, `g_mu=partial_mu theta-q_phi A_mu`, and
```text
S = integral sqrt(-g) d4x [ -F^2/4 + (partial W)^2/2 + W^2 g^2/2
    - lambda(W^2-W0^2)^2/4 + L_B(N,M*(W)) - g_omega A_mu J_B^mu + L_spectator ].
```
`L_B=bar N(i gamma^mu nabla_mu-M*(W))N` and `M*(W)=g_s W`, with `g_s=M_N/W0`; the displayed vector-current sign defines the charge convention. `L_spectator` may contain only passport-complete maintained kinematics needed for neutrality/beta equilibrium, not a new W interaction. No independent Proca term, density operator, or transfer vertex is present.

The source-free vacuum has `J_B=J_ext=0`, no phase chemical potential, and the invariant condition `g_mu=0`; in unitary gauge take `theta=0`, `A_mu=0`, `W=W0`. Thus `mu^2=lambda W0^2` (`mu=880.213 MeV` for the maintained `lambda,W0`) and the vacuum phase frequency is not an observable.

The vector mass is generated by this same scalar: `m_A(W)=|q_phi|W`. Bind `q_phi=m_omega/W0=0.911059`; it is derived, not fitted. Keep `g_omega` only as the distinct matter-current coupling. Reject `q=47.040476` as a Higgs charge: it would give `m_A=40407.77 MeV` and is neither a transfer normalization nor this action's vector calibration.

## Charge, stationary generator, and stress

The conserved global material quantity is baryon number. The local gauge current is, in the declared sign convention, `J_g^mu=q_phi W^2 g^mu+g_omega J_B^mu`; the homogeneous closed sector has `J_g^0=0`, so its condensate charge is `n_phi=W^2 g_0=-(g_omega/q_phi)n_B`. This Gauss constraint owns the compensating charge; it is not the identification `n_phi=n_B` and not charge transfer. The base ensemble fixes `N_B` along this gauge-neutral sector, with no independently varied scalar reservoir and no `mu_phi=mu_B` condition. Any transfer variant requires a new gauge-invariant vertex, its conserved charge, normalization, chemical-equilibrium law, and common stress, so it is outside this contract.

For homogeneous matter in unitary gauge, use the single stationary generator
```text
G = F_B(n_B,M*(W)) + lambda(W^2-W0^2)^2/4
    - q_phi^2 W^2 A0^2/2 + g_omega A0 n_B - mu_B n_B.
```
Its variations give `q_phi^2 W^2 A0=g_omega n_B` and
`lambda W(W^2-W0^2)+g_s n_s-W(q_phi A0)^2=0`. Eliminating `A0` gives the positive vector energy `g_omega^2 n_B^2/(2 q_phi^2 W^2)` and the equivalent W derivative `-g_omega^2 n_B^2/(q_phi^2 W^3)`. At `W=W0` the Gauss solution reduces to the maintained map `A0=g_omega n_B/m_omega^2`, but here it is dynamical and must be re-solved as W changes.

Metric variation of this covariant action is the only stress definition. After restricting to the neutral stationary sector, `P=-G_*`, `epsilon=G_*+mu_B n_B`, and `P=mu_B n_B-epsilon`; `mu_B=partial epsilon/partial n_B` along that sector must include every scalar/vector density derivative. Density-dependent extensions, if ever authorized, must generate their rearrangement terms through this same functional. A separately finite-differenced pressure or scalar-only/sign-flipped stress is inadmissible.

## Executable and fail-closed claims

Phase 3 may execute the vacuum identities, mass calibration, coupled W/Gauss equations, positive eliminated vector energy, total current/Ward identity, Hilbert stress, and HVH/Legendre closure. It may scan the resulting positive-W homogeneous branch and compare it with legacy formulas. For `g_omega n_B != 0`, the bound homogeneous energy diverges as `W^-2`, so this action has no finite-energy `W=0` state; this is an action-level no-go, not a fitted threshold.

No supported current mechanism reverses that result near nuclear density: the declared scalar-density term changes W by about `-0.10%` at `n0` and leaves `W/W0=0.99757` at `2.5 n0`; the proposed positive density curvature is unmatched and acausal in its diagnostic; screening changes vector stiffness but not scalar curvature; transfer is unspecified; and the toy coexistence construction is at hundreds of `n0` without finite-size closure. Therefore no `2.5 n0` melting/bounce, density-induced `W->0`, equilibrium phase-transfer, or free-energy-transition claim is executable.

Every load-bearing input must carry producer, units, uncertainty, source/citation, scheme, scale, operator definition/transformation, covariance group, and metadata status. `q_phi` and `g_s` remain correlated derived quantities, and covariance propagates by the full Jacobian. The 37 blocked renormalisation records cannot define scalar operators, conversions, or an RG-invariant mass fraction. An incomplete passport may support an explicitly labelled scan, but empirical matching and public physical claims remain blocked; no missing coupling may be fitted to the legacy threshold.
