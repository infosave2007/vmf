# Граница фаз и граница применимости космологического действия

Продолжение [расчёта связывания](NVG_NUMERICAL_CLOSURE_AND_BINDING_RU.md)
и [однородной космологической проверки](NVG_BOUNCE_GLOBAL_COMPLETION_RU.md).
Дата: 2026-09-09. Числа насыщения остаются входами прежней обратной
конструкции; в этой работе они не меняются. Новые выводы относятся к
указанным ниже математическим приближениям, не к экспериментальному
подтверждению NVG или обнаружению новой частицы.

## 1. Пространственное поле нельзя заменить однородной формулой

Используются фиксированные параметры `BindingDesign`, плоская статическая
граница фаз и нормальное приближение Томаса — Ферми при нулевой
температуре. В нём локально заполняются состояния фермионов; море Дирака,
спаривание, кулоновское поле и квантовые градиентные поправки не добавлены.
Самосогласованные неоднородные расчёты этого типа — известный метод,
а не новая физическая гипотеза этой работы
([Zhang–Shen, 2014](https://arxiv.org/abs/1405.3847)).

При сигнатуре +--- положим W=W(z)>0, A=A₀(z), m=gₛW,
ν=μ₀−gωA. Тогда n=d(ν²−m²)³ᐟ²/(6π²) при ν>m и n=0 в противном
случае. Это физический порог заполнения, а не обрезание ошибки решателя.

Большой термодинамический потенциал на единицу площади:

\[
\Omega[W,A]=\int dz\left[
\frac{W'^2-A'^2}{2}+U(W)
-\frac{q_\phi^2W^2A^2}{2}-P_F(\mu_0-g_\omega A,g_sW)\right].
\]

По W ищется минимум, **по A — максимум**. Отрицательный знак A′²
в этом функционале не является отрицательной физической энергией
распространяющейся частицы: A₀ — поле связи, подлежащее исключению
через уравнение Гаусса. При заданной плотности его физический вклад равен

\[
E_A=\frac12\left\langle g_\omega n,
(-\partial_z^2+q_\phi^2W^2)^{-1}g_\omega n\right\rangle\ge0.
\]

Этот оператор нелокален. Замена на gω²n²/(2qφ²W²) допустима для
однородного поля, но не для произвольной границы фаз.

Правильные пространственные уравнения:

\[
W''=U_W+g_s n_s-q_\phi^2WA^2,\qquad
-A''+q_\phi^2W^2A=g_\omega n.
\]

При V₍grand₎=U−qφ²W²A²/2−P_F сохраняется напряжение
(W′²−A′²)/2−V₍grand₎. У бесконечной границы двух сосуществующих
фаз оно равно нулю.

Для смешанной производной важно учитывать **обе** зависимости
ν=μ₀−gωA и m=gₛW:

\[
\frac{\partial^2 V_{\rm grand}}{\partial W\,\partial A}
=-2q_\phi^2WA+g_\omega g_s P_{F,\nu m},\qquad
P_{F,\nu m}=-\frac{d\,m\sqrt{\nu^2-m^2}}{2\pi^2}.
\]

На контрольной жидкой фазе y=0.75 результат составляет примерно
−3.10689×10⁵ МэВ²; фермионный член даёт −2.84252×10⁵ МэВ².
Его пропуск означает ошибку около 91.5% полной величины. Раньше
символическое сравнение двух порядков дифференцирования могло не
заметить этот пропуск, потому что в обоих порядках ν и m оставались
независимыми переменными. Исправленная физическая производная проверяется
независимым дифференцированием полного численного потенциала. Само
совпадение двух одинаково неполных формул не доказывает правильности.

## 2. Строгий коридор поверхностной энергии

Пусть A_L(W) максимизирует локальную часть функционала при заданном W.
Для прежней конструкции этот максимум равен барьеру

\[
B(W)=\beta(y^2-1)^2(y^2-y_*^2)^2,\qquad y=W/W_0.
\]

Обозначим a_F=k_F²/(3nE_F), b=gₛm/E_F, M²=qφ²W². Неявное
дифференцирование локального уравнения Гаусса даёт

\[
\frac{dA_L}{dW}=-\frac{g_\omega b+2a_FM^2A_L/W}
{g_\omega^2+a_FM^2}.
\]

На отрезке W*≤W≤W₀ достаточно константы

\[
\eta=\max\left(\frac{g_s}{g_\omega},
\frac{2(\mu_0-g_sW_*)}{g_\omega W_*}\right)<1.
\]

Тогда для профилей, остающихся на этом отрезке,

\[
\int dz\left[\frac{1-\eta^2}{2}W'^2+B(W)\right]
\le\sup_A\Omega[W,A]
\le\int dz\left[\frac12W'^2+B(W)\right].
\]

Нижняя граница получается подстановкой допустимого A=A_L(W),
верхняя — удалением отрицательного A′² и локальной максимизацией.
Направления неравенств существенны.

После минимизации по W получаем

\[
\boxed{\sqrt{1-\eta^2}\,\sigma_0\le\sigma\le\sigma_0},\qquad
\sigma_0=W_0\sqrt{2\beta}\,\frac{2}{15}
(1-y_*)^3(y_*^2+3y_*+1).
\]

Это энергия на площадь в естественных единицах МэВ³; для МэВ/фм²
нужно разделить на (ℏc)². Интеграл для σ₀ вычисляется точно, поскольку
на y*≤y≤1 квадратный корень из барьера — полином.

Это **ограниченная** вариационная теорема: она не доказывает, что
произвольная конфигурация с W>0 не уйдёт ниже W*. Проверка такой
возможности должна использовать настоящий оператор Гаусса. Положительные
значения на конечной сетке проб не могут заменить глобальное доказательство.

При фиксированных входах прежней обратной конструкции η≈0.157805,
а численные значения строгих границ составляют

\[
\boxed{3.3950\le\sigma\le3.4382\quad
\text{МэВ/фм}^2}.
\]

Границы здесь округлены наружу, чтобы сохранить знак неравенства.

Совместное решение уравнений W и A на конечном отрезке даёт
**около 3.41 МэВ/фм²**, внутри этого коридора. Это приближённое
стационарное решение, не доказательство достижения глобального минимума.
При уточнении расчёта относительное изменение энергии составило около
0.13%; независимая безразмерная невязка уравнений — до 2.61×10⁻⁵.
Проверены также сохранение напряжения и малость производных у границ.
Конечная область и точечные проверки невязки не дают строгой оценки
ошибки всей бесконечной стенки.

Четыре дополнительных периодических пробных профиля, в том числе с
W<W*, рассчитаны с нелокальным оператором Гаусса на двух сетках.
Отрицательного избытка энергии в них не найдено. Это ограниченный поиск
контрпримера, а не доказательство неотрицательности всех конфигураций.

## 3. Что переносится на новый потенциал в однородной космологии

Для неотрицательного потенциала и положительной векторной связи

\[
\rho=\frac{\dot W^2}{2}+E(n,W),\qquad
Q\equiv\rho+P=\dot W^2+nE_F+\frac{G_vn^2}{W^2}>0
\]

при n>0, причём Q≤2ρ. Производные полного E(n,W), а не только
равновесной ветви, нужны для сохранения энергии движущегося поля.
Неотрицательный новый U растёт как W⁸, поэтому ограниченная плотность
энергии ограничивает W сверху. Векторная энергия ограничивает W снизу
при фиксированной положительной n. Эти оценки относятся к однородной
модели с ранее **добавленным** законом гравитации.

В обычной плоской ОТО, напротив,

\[
\dot H=-\frac{\rho+P}{2M_{\rm Pl}^2}\le0.
\]

Поэтому замена одного потенциала вещества не создаёт переход H<0 → H>0.
Это ограничение класса действий, а не численная неудача пересчёта.

## 4. Кривизна в том же добавленном действии

Обозначим M=M₍Pl₎ и α²=3/(4M²ρ_c). Для ранее реконструированного
отрицательного cuscuton-сектора

\[
S_{\rm add}=\int\sqrt{-g}\,[-\sqrt{(\partial\Psi)^2}
-\mathcal V(\Psi)]\,d^4x,
\]

ветвь с ненулевым времениподобным градиентом задаётся

\[
\alpha\Psi=q+\tfrac12\sin2q,\quad
\mathcal V=-\rho_c\cos^4q,\quad
H=\frac{\alpha\rho_c}{3}\sin2q.
\]

Его уравнения с пространственной кривизной κ, а не угаданная поправка
к плоскому закону, имеют вид

\[
\boxed{\rho=\rho_c\cos^2q+\frac{3M^2\kappa}{a^2}},\qquad
\boxed{\dot q=\alpha\left(Q-\frac{2M^2\kappa}{a^2}\right)},
\qquad\dot a=Ha.
\]

При κ=0 возвращается прежний плоский результат. Добавленная
гравитация и масштаб ρ_c не следуют из исходного действия вещества NVG.
Сам механизм cuscuton-отскока уже изучен в литературе
([Boruah и соавт., 2018](https://arxiv.org/abs/1802.06818)).

Для κ=+1 и исходного стационарного баротропного EOS с P≥0 положим
F(a)=ρ(a)−3M²/a². На F≥0 имеем Q≥ρ≥3M²/a², поэтому

\[
F'(a)=-\frac{3Q}{a}+\frac{6M^2}{a^3}<0.
\]

Формальная траектория определяется F(a)=ρ_c cos²q. Минимальный
масштаб соответствует F=ρ_c, максимальный — F=0. Это позволяет
проверять периодическое движение фона, но ещё не регулярность действия.
Баротропный пример также не является доказательством периодичности
полного динамического скалярного поля W.

## 5. Почему формального цикла недостаточно

При q=π/2 обращается в нуль dΨ/dq=2cos²q/α. Если q̇ остаётся
конечной, то Ψ̇=0. Для δ=q−π/2:

\[
\Psi-\Psi_{\rm end}=\frac{2\delta^3}{3\alpha}+O(\delta^5),
\qquad\mathcal V=-\rho_c\delta^4+O(\delta^6).
\]

Следовательно, V как функция Ψ имеет поведение −const·|Ψ−Ψend|⁴ᐟ³:
оно C¹, но не C². Нормаль ∂μΨ/√((∂Ψ)²) также не определена
на Ψ̇=0 без дополнительного предписания. Гладкое продолжение
однородной кривой q(t) само по себе не решает эту проблему.

Это не обычная координатная трудность при H=0: здесь исчезает сам
градиент вспомогательного поля. В исследованиях регулярного
cuscuton-отскока условие ненулевого градиента оговаривается отдельно
([Quintin–Yoshida, §II.2, примечание 6](https://arxiv.org/html/1911.06040v2)).
Возможность слабого либо расширенного решения требует отдельного
определения и проверки; утверждение о здоровом цикле из этого не следует.

Простое добавление положительного кинетического члена проблему
автоматически не исправляет. При X=(∂χ)²>0 и
P=−μ_c²√X+εX/2−V, ε>0, получаем

\[
P_X+2XP_{XX}=\frac\epsilon2>0,\qquad
P_X=\frac\epsilon2-\frac{\mu_c^2}{2\sqrt X}<0
\quad\text{при }\sqrt X<\mu_c^2/\epsilon.
\]

У восстановленной распространяющейся степени свободы положительная
временная кинетика, но неправильный знак пространственного градиента.
Это препятствие именно данному способу регуляризации, а не теорема
о невозможности всех модификаций гравитации.

## 6. Гладкая поправка, сохраняющая форму отскока

Есть конструктивный способ устранить конкретное обнуление градиента:
изменить **вместе** потенциал и связь Ψ с q. Введём явно заданное ε>0:

\[
\boxed{\alpha\Psi=(1+\varepsilon)q+\tfrac12\sin2q},\qquad
\boxed{\mathcal V=-\rho_c\cos^2q(\cos^2q+\varepsilon)}.
\]

Это новая гипотеза гравитационного сектора. Она не подменяет прежнее
действие и не выведена из микрофизики NVG. В частности, здесь **не**
добавляется εX/2: кинетический cuscuton-член остаётся прежним. Параметр ε
не является найденной из наблюдений константой.

Производная dΨ/dq=(ε+2cos²q)/α теперь строго положительна. При каждом
фиксированном ε>0 обратная функция q(Ψ) и потенциал V(Ψ) аналитичны.
При этом сохраняется H=αρ_c sin2q/3, а из действия следуют

\[
\rho-\frac{3M^2\kappa}{a^2}=(1+\varepsilon)\rho_c\cos^2q,
\qquad
\dot q=\frac\alpha{1+\varepsilon}
\left(Q-\frac{2M^2\kappa}{a^2}\right).
\]

В замкнутой баротропной модели с P≥0 при максимальном расширении

\[
\boxed{\dot\Psi_{\rm turn}=\frac{\varepsilon}{1+\varepsilon}
\left(Q-\frac{2M^2}{a^2}\right)>0}.
\]

Следовательно, именно прежняя проблема Ψ̇=0 в этой конструкции
устраняется на однородном фоне. Это не доказывает устойчивости всех
возмущений, совместимости с наблюдениями или периодичности Вселенной
с образованием звёзд и ростом энтропии.

Особенно просто получается плоский закон:

\[
\boxed{H^2=\frac{\rho}{3M^2(1+\varepsilon)}
\left(1-\frac{\rho}{(1+\varepsilon)\rho_c}\right)}.
\]

Форма отскока сохранилась. Изменились космологический коэффициент
гравитации и плотность отскока:

\[
\boxed{M_{\rm cosm}^2=M^2(1+\varepsilon)},\qquad
\boxed{\rho_{\rm bounce}=(1+\varepsilon)\rho_c},\qquad
\boxed{G_{\rm cosm}/G_{\rm bare}=1/(1+\varepsilon)}.
\]

Например, ε=0.01 означало бы G₍cosm₎/G₍bare₎=100/101≈0.990099.
Это арифметическое следствие выбранного входа, не измерение. В кривом
случае в F=ρ−3M²κ/a² остаётся **исходное** M; заменять его на M₍cosm₎
нельзя. Низкоплотностный коэффициент при κ/a² также меняется на 1/(1+ε).

По одной плоской зависимости H²(ρ) различимы лишь комбинации
M²(1+ε) и ρ_c(1+ε). Если оба исходных масштаба считать свободными,
из такого графика нельзя независимо измерить ε. Для проверки поправки
нужны дополнительные сведения о локальной гравитации, кривизне либо
возмущениях. Совпадение формы кривой само по себе не подтверждает новое
действие.

Эта цена связана с общим свойством гладкого максимума. Если
V(Ψend)=V′(Ψend)=0, V″(Ψend)=−K<0, K конечно, то H=V′/3 и
ρ=3M²H²−V дают

\[
\boxed{\frac{H^2}{\rho}\longrightarrow
\frac{1}{3\left(M^2+3/(2K)\right)}}.
\]

Сам эффект изменения космологической планковской массы известен
([Afshordi и соавт., 2007, формула (14)](https://arxiv.org/html/astro-ph/0702002)).
Здесь он независимо выводится из локального разложения и применяется
к конкретной поправке. Для неё K=3/(2M²ε), что воспроизводит формулу выше.
Равенство G₍bare₎ реально измеренной локальной постоянной требует
отдельной гравитационной проверки; это не готовое наблюдательное
исключение или подтверждение модели.

При малом положительном ε изменение может быть малым, но **точный**
возврат к исходному коэффициенту этой семьи требует ε→0. В этом пределе
кривизна потенциала K расходится, а Ψ̇ при развороте снова обращается
в нуль. Поэтому регулярность при каждом ε>0 не является равномерным
доказательством слабой связи или устойчивости возмущений при ε→0.
Вырожденный максимум K=0 также не входит в приведённую теорему.

## 7. Численная проверка и воспроизведение

Для гладкой гравитационной поправки рассчитаны два **искусственных
контрольных примера**: P=wρ с w=0 и w=1/3, ε=0.1, ρ_c/ρ_ref=1.
Они не подменяют EOS вещества NVG и не задают возраст нашей Вселенной.
Единицы: a_ref=√(3M²/ρ_ref), A=a/a_ref, τ=t/a_ref, c=ℏ=1.
Плотность при максимальном расширении равна ρ_ref, поэтому A_max=1
является выбором нормировки, а не предсказанием.

| Контроль | A_min | Период по квадратуре, Δτ | Период по прямому интегрированию, Δτ |
|---|---:|---:|---:|
| w=0 | 0.6696616574 | 2.85934415736 | 2.85934415736 |
| w=1/3 | 0.7757078901 | 1.72866031464 | 1.72866031464 |

Второй способ интегрирует A и q по времени, не подставляя в уравнение
готовую кривую A(q). Максимальное расхождение масштаба A с независимо
обращённым ограничением на проверенных точках — около 1.44×10⁻¹⁴ и
4.26×10⁻¹⁴ соответственно. Это наблюдаемое согласие численных методов,
не строгая интервальная оценка ошибки всей непрерывной траектории.
Минимальные Ψ̇/ρ_ref равны примерно 0.0303030 и 0.0606061 и положительны.
Положительность на всём баротропном цикле также следует из аналитических
оценок Q≥ρ≥3M²/a² и dΨ/dq≥ε/α; она не выводится только из выборки.

**Финальная совместная приёмка: 178 тестов прошли в 11 связанных
модулях, 190.90 с в использованном локальном окружении.** Это выбранная
регрессия затронутых расчётов, не заявление о запуске каждого теста
репозитория. Все три новые программы дополнительно запущены через CLI;
их JSON проверен. Пространственный CLI повторён с `--dps 120`.
Высокая точность относится к аналитическим константам и mpmath-проверкам;
решатель границы фаз остаётся обычным численным BVP с указанной выше
конечной точностью.

Разделённая реализация и приёмка через Lunacy помогли обнаружить то,
чего не выявляли ранние зелёные тесты: подмену реального потенциала
контрольным полиномом, пропущенное цепное правило, неполные проверки
согласия периодов и потерю точности при сравнении производных.
Эти дефекты исправлены; параметры вещества и ранее сохранённые
результаты для их устранения не перенастраивались.

Дополнительные проверки намеренно удаляют обязательные условия,
меняют знаки и значения, вводят NaN и большие расхождения периодов.
Приёмка отклоняет такие результаты. Отдельно проверено отклонение
ошибки смешанной производной 10⁻²⁰ при критерии 10⁻²⁵ без округления
к обычному `float`, а также штатный JSON-переход для 80/120 знаков.
Реестр проверен; девять прежних контрольных артефактов сохраняют свои
SHA-256. Хеши подтверждают неизменность файлов, не физическую теорию.

Воспроизвести новые расчёты из корня репозитория, без записи таблиц
результатов:

```sh
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=verification python3 -B verification/nvg_spatial_binding_audit.py --dps 120
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=verification python3 -B verification/nvg_bounce_potential_audit.py
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=verification python3 -B verification/nvg_regular_cyclic_action_audit.py
```

Скрипты печатают рассчитанный JSON. Заданные контрольные входы, названия
обязательных проверок и пределы численной ошибки не являются таблицами
готовых физических ответов. Числа насыщения в `BindingDesign` остаются
калибровочными входами; согласие с ними не считается новым независимым
подтверждением.

Связанные проверки:

```sh
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=verification python3 -B -m pytest -p no:cacheprovider -q \
  verification/test_nvg_spatial_binding_audit.py \
  verification/test_nvg_bounce_potential_audit.py \
  verification/test_nvg_regular_cyclic_action_audit.py \
  verification/test_nvg_binding_feasibility_audit.py \
  verification/test_nvg_bounce_global_structure_audit.py \
  verification/test_covariant_bounce_completion_audit.py \
  verification/test_bounce_action_derivation_audit.py \
  verification/test_nvg_longitudinal_fluid_audit.py \
  verification/test_nvg_collisionless_response_audit.py \
  verification/test_nvg_response_bridge.py \
  verification/test_repository_organization.py
```

## 8. Что осталось открытым

Эта работа даёт пространственную оценку и согласованный вариант
однородных космологических уравнений. Она не завершает единую теорию.
По-прежнему нужны неограниченная пространственная устойчивость вещества,
возмущения и нелинейная устойчивость новой гравитации, её локальный
гравитационный предел и сопоставление с независимыми наблюдениями.
Регулярный космологический фон также не является решением внутренности
чёрной дыры: для неё нужны собственные неоднородные уравнения и условия
сшивки. Работоспособный энергетический генератор из этих результатов
не следует.

Продолжение: [расчёт линейных возмущений и устойчивости полного цикла](NVG_CYCLIC_PERTURBATION_CLOSURE_RU.md).
В нём выведено условие положительности плоского скалярного коэффициента,
учтена кривизна в скалярном действии и найдены три растущие тензорные
моды в фиксированных циклических контролях. Гладкость фона сама по себе
не гарантирует устойчивость повторяющихся циклов.
