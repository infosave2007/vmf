# Статический отклик среды и идентифицируемость масштаба `W0`

## Короткий итог

Продолжен расчёт из фиксированного действия NVG: к однородной
термодинамике добавлена **условная локальная статическая** ответная функция
среды Томаса—Ферми при нулевой температуре. Это не новая подтверждённая
физика и не эмпирический результат: вес независимого свидетельства равен
нулю. Воспроизводимый производитель печатает строгий JSON:

```text
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=verification python3 verification/nvg_spatial_identifiability_audit.py
```

Код не читает сохранённую таблицу результата и не создаёт файл результата.

## Что именно считается

Используются естественные единицы: `n` измеряется в МэВ³, `y=W/W0`
безразмерно, волновое число `k` — в МэВ, а `Z=W0²` — в МэВ². Для
возмущения Фурье до исключения временной компоненты векторного поля:

\[
H=\begin{pmatrix}
 a&b&g\\
 b&d_0+Z_s k^2&h\\
 g&h&-(t_0+k^2)
\end{pmatrix},\qquad Z_s=(sW_0)^2.
\]

Здесь
\[
a=F_{nn},\quad b=F_{ny},\quad
 d_0=F_{yy}+U_{yy}-m_\omega^2A_0^2,
\]
\[
g=g_\omega,\quad h=-2m_\omega^2yA_0,\quad
 t_0=m_\omega^2y^2,
\qquad A_0=\frac{g_\omega n}{m_\omega^2y^2}.
\]

Минус в компоненте `A0` — седловая структура ограничительного уравнения
Гаусса, а не признак отрицательной энергии распространяющегося призрака.
После исключения `A0`, при `L=t0+k²`, получаем

\[
D=a+\frac{g^2}{L},\qquad B=b+\frac{gh}{L},\qquad
C=d_0+\frac{h^2}{L}+Z_s k^2,
\]
\[
S=D-\frac{B^2}{C}.
\]

Число
\[
\chi=\frac{1}{S}
\]
публикуется только при `C>0` и `S>0`. Это отклик на внешний источник,
входящий как `-\mu_ext n`: `delta n=chi*delta mu_ext`. Если источник
записан как `+V_ext n`, то обычный знак отклика равен
`delta n/delta V_ext=-chi`.

Каждая строка также решает исходную матрицу `H u=(1,0,0)` напрямую;
первая компонента должна совпасть с `chi`. Поэтому проверяется не только
повторное применение формулы Шура. Смешанная производная полного
неисключённого функционала численно дифференцируется независимо на каждом
из шести фонов; контроль с пропущенным или перевёрнутым `h` отвергается.

## Где проверена зависимость от масштаба

Рассмотрено коррелированное семейство
\[
W_0\to sW_0,\qquad g_s\to g_s/s,\qquad q_\phi\to q_\phi/s,
\]
при фиксированных `M_N`, `m_omega`, `g_omega` и `U(y)`. Для Q4
`lambda -> lambda/s⁴`; для двух полиномов W8 коэффициенты в `y` оставлены
неизменными. Кандидатные `A`, `Cv`, массы, связи и градиентный коэффициент
пересчитываются из фактических входов, а не подставляются из исходной
строки.

Однородная часть остаётся нечувствительной к `s`, но канонический градиент
становится `Z_s=s²Z`. На общей устойчивой области проверяется тождество
\[
S(s_2)-S(s_1)=
\frac{B^2Zk^2(s_2^2-s_1^2)}{C_1C_2},
\]
и следствия
\[
\frac{dS}{d(s^2)}=\frac{B^2Zk^2}{C^2}\ge0,
\qquad
\frac{d\chi}{d(s^2)}\le0.
\]
При `k=0` или `B=0` ответ слеп к масштабу. Например, при `s=1.3`
отношение градиентных коэффициентов равно `1.69`; это не симметрия полного
действия, потому что каноническая пространственная динамика изменилась.

Физическая каноническая масса скалярной моды вычисляется из фактического
вакуумного изгиба потенциала:

\[
m_\sigma^2=\frac{U_{yy}(1)}{W_0^2}
 =\left.\frac{d^2U(W/W_0)}{dW^2}\right|_{W=W_0}.
\]

Для Q4 это совпадает с частным случаем `sqrt(2*lambda)*W0`, поскольку
`U=A*(y²-1)²/4` и `A=lambda*W0⁴`. Для W8 фактический потенциал задаётся
сохранёнными коэффициентами полинома `z²,z³,z⁴` и его кривизной; поля
`lambda` и вычисляемое из него `A` внутри общего `BulkModel` там являются только
неиспользуемыми вспомогательными значениями и не выдаются как физические
параметры W8. Метаданные масштаба помечают это явно и сохраняют коэффициенты
полинома.

### Обратное восстановление

Если остальные коэффициенты и измеренная статическая `chi(k)` действительно
известны, то в невырожденной устойчивой строке можно проверить
\[
Z_s=\frac{B^2/(D-1/\chi)-(d_0+h^2/L)}{k^2}.
\]
Производитель отказывается от восстановления при `k=0`, в точке `B=0`, при
неположительном знаменателе, неустойчивой строке или плохом/неположительном
числе. Обе чувствительные к сокращению разности должны пройти один и тот же
порог численной точности `1e-45`:

\[
r_{den}=\frac{|D-1/\chi|}{\max(|D|,|1/\chi|,10^{-100})}>10^{-45},
\]
\[
r_{sub}=\frac{|B^2/(D-1/\chi)-(d_0+h^2/L)|}
{\max(|B^2/(D-1/\chi)|,|d_0+h^2/L|,10^{-100})}>10^{-45}.
\]

При нарушении любого порога возвращается отказ численной обусловленности,
а не искусственно огромное или меняющее знак `Z_s`. Это только защита
сохранённых знаков арифметической точности, а не оценка экспериментальной
погрешности: физическое восстановление всё равно требует заданных ошибок и
их ковариации. Совпадение нескольких неслепых `k` проверяет лишь эту условную
статическую TF-модель; оно не обещает, что эксперимент бесплатно выделит
`chi` или `W0`.

## Предобъявленная численная сетка

* базовая Q4-ветвь: `n/n0 = 0.37, 1, 2.75, 7.25`;
* две существующие обратные трёхцелевые альтернативы W8: `y*=0.90` и
  `y*=0.93` при `n0`; цели построения — `0.16 fm^-3`, связь `-16 MeV`,
  нулевое давление и `K=240 MeV`;
* масштабы `s=0.5, 1, 1.3, 2`;
* `k=0, 100, 200 MeV`.

Всего в JSON явно присутствуют `6 × 4 × 3 = 72` строки. Статусы устойчивых,
неустойчивых и сингулярных ответов не отбрасываются. Для этой заявленной
сетки получены 72 устойчивые строки; это классификация выбранных условных
чисел, не доказательство устойчивости всех плотностей и всех волн.

При `k=0` проверяется совпадение с релаксированной однородной производной
`d mu_B/d n`; это гранканонический длинноволновой предел. Точная однородная
мода запрещена при фиксированном полном числе частиц `N`, хотя ненулевые
моды с нулевым средним остаются возможными.

Положительные слепые волновые числа найдены для базовой Q4-ветви:

| фон | `k_blind`, МэВ | `k_F`, МэВ | смысл |
|---|---:|---:|---|
| `2.75 n0` | `426.4314752` | `368.5256298` | формальная точка `B(k)=0` |
| `7.25 n0` | `1257.3056362` | `509.0996534` | формальная точка `B(k)=0` |

Для `0.37 n0`, `n0` и обеих W8-альтернатив положительного слепого `k` нет.
Особенно точка при `7.25 n0` лежит уже в области больших импульсов: это
скалярное разъединение внутри локальной TF-алгебры, **не прозрачность,
не резонанс и не достоверное all-k предсказание**.

На устойчивой ветви `C_y>0` знак этого исключения связан с уже известной
однородной кривой: `dy/dn=-B(0)/C(0)`. Поэтому положительный конечный
слепой `k` возможен именно при `B(0)<0`, то есть при росте равновесного
`y(n)`, а не из-за нового взаимодействия.

## Что сохраняется из прежней проверки

Исходная Q4-параметризация по-прежнему не является ядерной точкой насыщения:
при `n0` она даёт давление около `18.7236 MeV/fm³` и энергию связи около
`+124.3175 MeV`, а не `0` и `-16 MeV`. Две W8-строки попадают в объявленные
цели только потому, что это обратные конструкции; они не приняты как
физические модели и не являются отложенной проверкой.

Статический TF-расчёт не включает квантовую нелокальность, динамику/RPA,
конечный размер ядра, изовекторный сектор, состав, кулоновское поле,
неопределённости параметров и разрешение прежней неоднозначности хвоста и
локального jet. Поэтому условная идентифицируемость градиентного `W0` не
становится универсальным законом или эмпирическим подтверждением теории.

## Численный пример: одинаковая однородная среда, разный отклик

При `n=n0`, `k=200 MeV` получен следующий **модельный**, а не измеренный,
отклик `chi` в MeV². Внутри каждой строки одновременно меняются `W0`,
`g_s`, `q_phi` и соответствующая параметризация потенциала по указанному
выше коррелированному закону — это не изменение одного входа при прочих
неизменных.

| модель | `s=1` | `s=1.3` | изменение `chi` |
|---|---:|---:|---:|
| Q4 | 5674.86254559 | 5674.73672349 | -0.002217% |
| W8 0.90 | 27280.87113158 | 24768.06601347 | -9.210868% |

Однородные энергия и давление внутри каждого семейства при этом одинаковы.
В частности, для W8 `0.90` рост масштаба на 30% меняет этот статический
пространственный отклик примерно на 9.21%. Между Q4 и W8 однородные свойства
не обязаны совпадать. Импульс 200 MeV выбран для численной демонстрации:
без оценки квантовых поправок к локальному TF это не точный прогноз опыта.

Практический исследовательский смысл: нужны измерения реакции на
неоднородность, а не только новые совпадения однородной энергии/давления.
При известных остальных коэффициентах восстановленное `Z_s` должно
совпасть для нескольких неслепых волновых чисел. Сейчас нет ни прямого
экспериментального `chi(k)` с ошибками, ни полного перехода к наблюдаемой
конечного ядра; экспериментальная проверка остаётся следующим этапом.

## Проверки этого этапа

После последних исправлений прошли 13 новых тестов. До узких исправлений
также прошли 43 теста неизменённых foundation/scaling зависимостей; полный
исторический набор не запускался. Живой расчёт содержит 72 строки и
сравнение 522 величин при 80/110 десятичных разрядах. Это контроль численной
арифметики, не точность физических параметров.

Независимый расчёт через интегрирование по импульсу и размерное поле `W`
совпал в 56 сравнениях отклика/гессиана для 14 точек; максимальное
нормированное отличие — около `4.1e-60`. Отдельно сверены шесть физических
масс через прямое дифференцирование действительного потенциала. Испорченные
числа, единицы, состав строк, идентификатор исходного кода и статус
эмпирической проверки отвергаются. Восстановление масштаба при численной
потере значимости около `k=0` тоже отвергается.

Общий реестр сохраняет три прежние ошибки связей капельных документов;
новые записи этого этапа проходят проверку связей. Это не заявление о
прохождении всего репозитория.
