# Универсальность плотностного хвоста и невидимая калибровочная деформация

Этот отчёт фиксирует отдельную математическую проверку однородного
холодного функционала NVG. Расчёт выполнен заново программой
`verification/nvg_density_universality_audit.py`; вес независимых данных
равен нулю. Пределы при огромной плотности — формальные продолжения модели,
а не предсказание нейтронной звезды или Вселенной.

## Что именно проверено

После исключения однородного векторного поля используется

\[
 \varepsilon(n,y)=F_F(n,g_sW_0y)+U(y)+{C_vn^2\over2y^2},
 \qquad y={W\over W_0},
\]

Здесь `W=W0 y` — размерное поле, `U_W(W)=U(y)` — тот же потенциал,
переписанный в размерной переменной, а

\[
 \varepsilon(n,W)=F_F(n,g_sW)+U_W(W)+{G n^2\over2W^2},
 \qquad G=C_vW_0^2.
\]

Если `U_W(W)~a_W W^p`, то коэффициент в переменной `y` равен
`a_y=a_W W0^p` и `U(y)~a_y y^p`; это одна и та же размерная асимптотика,
а не два независимых коэффициента. В формулах ниже `a_W` относится к `W`,
`a_y` — к `y`, и выбор переменной не меняет физический потенциал.

Плотность `n` измеряется в естественных единицах `MeV^3`, а энергии и потенциал —
в `MeV^4`. Сохранены исходные `W0=859 MeV`, `MN=939 MeV`, `d=4`,
`hbar*c=197.3269804 MeV fm`, все связи и единица плотности `n0=.16 fm^-3`.
Для пяти случаев решено уравнение `E_y=0` на
`n/n0=10^2,10^4,10^8,10^12,10^18,10^24,10^36` с положительным локально
устойчивым корнем. Корни брались логарифмическим расширением скобки и
бисекцией; число найденных смен знака — нижняя диагностическая граница, а
не утверждение о полном наборе корней.

### Условная асимптотическая теорема внутри фиксированного класса

Предположим `p>0`, положительные постоянные связи `g_s,C_v` и фиксированный
набор фермионных видов/вырождения. Нужна положительная область `W>0`,
существующая локально устойчивая (для предела — выбранная глобальная)
ветвь минимума и гладкая асимптотика потенциала вместе с производными,
достаточная для дифференцирования. Для глобального утверждения функционал
`E(n,W)` должен быть ограничен снизу и коэрцитивен на всех доступных концах
области: при `W->0+` это означает, в частности, что никакая отрицательная
сингулярность не пересиливает `G n^2/(2W^2)`, а при `W->infinity`
положительный хвост `a_WW^p` должен вести к `+infinity`. Одного
положительного большого-W хвоста недостаточно: например,
`U_W=a_W W^p-b/W^3`, где `b>0` имеет единицы `MeV^7`, даёт
`E->-infinity` при `W->0+` (отрицательный `W^-3`
сильнее положительного векторного `W^-2`), поэтому минимума может не быть.

Для `p>4`, если `U_W~a_WW^p`, баланс векторного и скалярного хвостов даёт

\[
 W_{as}=\left({G\over pa_W}\right)^{1/(p+2)}n^{2/(p+2)},\qquad
 \varepsilon_{as}=\left(1+{p\over2}\right)a_W
 \left({G\over pa_W}\right)^{p/(p+2)}n^{2p/(p+2)}.
\]

Действительно, при `W=n^(2/(p+2))z` и делении на
`n^(2p/(p+2))` ведущая функция есть
\[
 \Phi_p(z)=a_Wz^p+{G\over2z^2}.
\]
Она коэрцитивна при `z->0+` и `z->infinity`, а для `p>4`
`\Phi_p''=p(p-1)a_Wz^(p-2)+3Gz^-4>0`; значит, её единственный минимум
`z*=(G/(pa_W))^(1/(p+2))` не вырожден. Если остаток вместе с нужными
производными стремится к нулю равномерно около `z*`, положительная
кривизна и теорема о неявной функции дают гладкую продолженную ветвь и
легитимное дифференцирование асимптотики. Это условие сильнее, чем один
логарифмический наклон энергии.

Тогда формально `w∞=c_th²∞/c²=(p-2)/(p+2)`, `U/ε→2/(p+2)`,
`V/ε→p/(p+2)`, `(ε−3P)/ε→2(4−p)/(p+2)`. Условия подавления фермионного
вклада имеют степени
`W/kF ~ n^((4-p)/(3(p+2)))`,
`F/epsilon ~ n^(2(4-p)/(3(p+2)))` и
`(matter derivative)/(vector derivative) ~ n^(4(4-p)/(3(p+2)))`.

Для `0<p<=4` надо положить `W=n^(1/3)z`. После деления на `n^(4/3)`
получается
\[
 \Phi_{\le4}(z)=\phi(g_sz)+{G\over2z^2}
       +\mathbf 1_{p=4}a_Wz^4,
 \qquad F_F(n,m)=n^{4/3}\phi(m/n^{1/3}).
\]
При `z->0+` векторный член расходится, а при `z->infinity` Fermi-член
(при `g_s>0`) и, если `p=4`, quartic обеспечивают рост; их сумма
коэрцитивна. Строгая выпуклость фермионной части следует из
`d² sqrt(k²+m²)/dm²=k²/(k²+m²)^(3/2)>0` под интегралом по заполненной
ферми-сфере; векторный член и член четвёртой степени также выпуклы. Поэтому имеется
единственный положительный невырожденный
минимум.
Равномерные `C^r`-остатки потенциала дают ту же гладкость ветви через
теорему о неявной функции. Для `p<4` потенциал в этом масштабе
сублидирующий, а для `p=4` он остаётся ведущим; в обоих случаях
`w∞=c_th²∞/c²=1/3`. Это полная Fermi/vector/quartic-задача, а не замена
её расчётом без фермионного вклада, дающим `z=2.21316126776` (полный расчёт
даёт `z=2.18530377508`).

Следовательно, при перечисленных предположениях компактная формула класса
при `p>0` такова:
\[
 \boxed{\displaystyle
 w_\infty=\lim_{n\to\infty}{c_{\rm th}^2\over c^2}
 =\max\!\left({1\over3},{p-2\over p+2}\right).}
\]
Здесь `c_th²=c²(dP/dε)_eq` — равновесная термодинамическая производная
вдоль релаксированной ветви; это не сертифицированная скорость
микроскопической распространяющейся моды и не результат RPA/линеаризации
полной динамики. Формула — математический результат внутри заявленного
однородного mean-field-класса, а не новый закон природы и не удаление
mean-field-приближения. `G=0`, бегущие связи, не степенной хвост,
отрицательный ведущий коэффициент, новые виды частиц или другая ветвь
требуют отдельного анализа.

Отдельный точный контроль степеней `p=2,4,6,8,16` явно отвергает подстановку
`(p−2)/(p+2)` для холодного `p=2`: там предел остаётся `1/3`, а не `0`.

| случай | хвост `p` | `w∞=(dP/dε)∞` | `P/ε` при `10^36` | `dP/dε` при `10^36` | отношение поля к асимптотике | отношение энергии |
|---|---:|---:|---:|---:|---:|---:|
| исходный quartic | 4 | 1/3 | 0.3333333333333333 | 0.3333333333333333 | 1+2.15·10⁻²⁴ | 1−7.35·10⁻²⁴ |
| W8, `y*=.90` | 8 | 3/5 | 0.5999999998729442 | 0.5999999998941196 | 1+6.15·10⁻¹⁵ | 1+4.76·10⁻¹⁰ |
| W8, `y*=.93` | 8 | 3/5 | 0.5999999998281135 | 0.5999999998567599 | 1+1.45·10⁻¹⁴ | 1+6.45·10⁻¹⁰ |
| U16 из `.90` | 16 | 7/9 | 0.7777777827147916 | 0.7777777820976649 | 1+9.72·10⁻⁹ | 1−2.22·10⁻⁸ |
| U16 из `.93` | 16 | 7/9 | 0.7777777860342353 | 0.7777777850021782 | 1+1.63·10⁻⁸ | 1−3.72·10⁻⁸ |

Низкая граница сетки ещё не является пределом: при `10^2` значения `P/ε`
равны примерно `.528` (quartic), `.425/.358` (W8) и `.441/.358` (U16),
поэтому таблица выше показывает только честно достигнутые маркеры `10^36`,
а не аппроксимацию степенным фитингом.

Все конечные строки прошли внутренние проверки стационарности, тождества
`ε+P=nμ`, тождества следа `ε−3P=4U−yU_y`, положительных `C=E_yy` и `μ`, а
также конечные разности по `ln n` при шагах `10^-5` и `5·10^-6` с ошибкой не
выше `10^-7`. Независимая реконструкция с 110 знаками для `10^4`, `10^36`
и калибровочных точек согласуется с основной 70-знаковой с относительной
ошибкой менее `1.4·10^-63` (требование было `10^-25`).

## Конструктивная неидентифицируемость

Для каждого принятого W8-примера заново восстановлен его положительный
старший коэффициент `a4`. К нему добавлена заранее заданная деформация

\[
 \Delta U(y)=a_4(y^2-1)^4(y^2-y_*^2)^4.
\]

Она неотрицательна при всех вещественных `y`, имеет степень 16 по `y` и
обнуляет `ΔU`, `ΔU'`, `ΔU''`, `ΔU'''` точно в вакууме `y=1` и в точке
`y=y*`. То есть «первые четыре jets» здесь означает порядки `0,1,2,3`,
а не четыре ненулевые производные. Поэтому в самой калибровочной точке
совпали энергия, давление, `μ`, скалярная кривизна и `K` (все шесть
относительных сравнений для обоих `y*` равны нулю в вычисленной точности),
но формальный хвост изменился `p=8 -> p=16`: `3/5 -> 7/9`.

Это намеренно положительный контрпример к идентифицируемости по конечным
локальным данным, а не предлагаемое исправление физики: `U16` не принят как
новое взаимодействие NVG и не подменяет исходную теорию. Полный исходный
quartic и калиброванные W8 `.90/.93` — разные потенциалы и разные теории;
совпадение выбранных производных в точке не подтверждает исходный потенциал.
Именно такая неоднозначность высокоплотного продолжения уже имеет прямую
предшествующую литературу о моделях, совпадающих при обычной плотности и
расходящихся на большой плотности: см. [Mueller--Serot (1996)](https://arxiv.org/abs/nucl-th/9603037).

Первый локальный различитель уже существует около калибровки и не требует
ухода к `10^36`. При `dy/dln n=-nB/C`:

\[
 \Delta U''''(y_*)=384a_4y_*^4(1-y_*^2)^4,\qquad
 \Delta\varepsilon''''(n_0)=\Delta U''''(y_*)
 \left({dy/d\ln n\over n_0}\right)^4.
\]

Для нормированного коэффициента
`Z=81 n0^4 d^4(ε/n)/dn^4` получены `ΔZ=2.1207879107 MeV` для `.90` и
`ΔZ=.04849382613 MeV` для `.93`. Это вычисленный диагностический коэффициент
выбранной гладкой однородной ветви, не измерение и не заявление о доступной
экспериментальной точности. Он не переносится автоматически на смесь фаз
после построения выпуклой оболочки энергии или на конечное ядро.
Локально, если `x=(n−n0)/n0` и `e=ε/n`, это означает
`Δe=ΔZ*x^4/1944+O(x^5)` (поскольку `81*4!=1944`): различитель относится
только к малой окрестности общей точки и не является оценкой расхождения
при 10% изменении плотности или утверждением о его измеримости.

При `p=4` полный предельный минимизатор дал `z=2.18530377508`, тогда как
вариант без фермионного вклада (только потенциал плюс вектор) дал
`z=2.21316126776`; относительное различие `1.2587·10^-2`. Это явный
контроль против незаметной замены холодного фермионного члена на
иной предельный функционал.

## Вириальная аналогия и гравитационная граница

В плотностном пределе `p>4` действует статическое тождество
`2 V_vector=p U`. Та же алгебра в быстро осциллирующем каноническом скаляре
имеет вид `2<T>=p<U>` и даёт такое же отношение давления к энергии при
своих предположениях. Это не равенство действий, спектров, возмущений или
динамики. В частности, при `p=2` холодный однородный предел здесь остаётся
Fermi/vector-доминированным с `w=1/3`, а осциллирующий квадратичный скаляр
имеет `w=0`. В качестве контекста общей вириальной формулы см. [Turner,
Phys. Rev. D 28, 1243 (1983)](https://doi.org/10.1103/PhysRevD.28.1243) и
[Boyle–Caldwell–Kamionkowski, astro-ph/0105318](https://arxiv.org/abs/astro-ph/0105318).

Для канонической динамики в неизменённой плоской ОТО при `Lambda0=0`
полное тождество имеет вид
\[
 \rho+P=\dot W^2+nE_F+{C_vn^2\over y^2}>0.
\]
Оно исключает плоский contraction-to-expansion bounce через
`\dot H=-(\rho+P)/(2M_P^2)<0` даже без предположения адиабатического
слежения. Статическая запись `ε+P=nμ>0`,
`μ=E_F+C_vn/y²`, — лишь специальный случай `\dot W=0` на равновесии.

Полезно не смешивать ядерную калибровочную величину `K=240 MeV` с
пространственно-временным инвариантом Кретчманна
`\mathcal K=R_{\mu\nu\rho\sigma}R^{\mu\nu\rho\sigma}`. Кроме того,
\[
 {\mathcal K\over(8\pi G_N)^2\rho^2}
 ={(1+3w)^2+4\over3}>0.
\]
В плоском FLRW при `Lambda0=0` также
`R=8pi G_N(ρ−3P)` и
`\mathcal K=12[(\dot H+H^2)^2+H^4]`. Поэтому
`(ρ−3P)/ρ->0` означает лишь исчезновение нормированного следа; это не
означает абсолютного `R->0`. Даже точная радиация с `R=0` оставляет
`\mathcal K` расходящимся при `ρ->infinity`. Для исходного quartic здесь
установлен только асимптотический нормированный предел следа, а не точное
тождество `R=0`.

Наконец, независимо проверенные условные диагностические отношения для
`p>4` (если равновесная энергия доминирует и `W` следует минимуму) таковы:
\[
 \dot W=-{6\over p+2}HW,\quad
 {T_{radial}\over\epsilon_{eq}}\sim{6W^2\over(p+2)^2M_P^2},\quad
 {C_W\over H^2}\sim{6pM_P^2\over W^2},\quad
 C_W\sim p(p+2)a_WW^{p-2}.
\]
Это предупреждающие proxy-критерии: `C_W` — замороженная плотностная
кривизна, не физическая собственная частота после смешивания с плотностью,
вектором и метрикой; здесь не выполнена новая симуляция. При сжатии `W`
растёт, а `H<0` даёт антифрикцию, так что подмена полной динамики статическим
EOS может терять самосогласованность по мере роста `W/M_P`; скорость этого
процесса здесь не вычислялась.

## Граница вывода

Это результат о формальных классах асимптотик и о конструктивной
неидентифицируемости по конечному набору локальных калибровочных данных.
Он не устанавливает глобальную выпуклость EOS, полноту всех стационарных
ветвей, физическую применимость при бесконечной плотности, конечные ядра,
квантовый RPA, космологическое слежение, частоты, чёрные дыры, циклы или
эмпирическое подтверждение. Положительная деформация — контрпример к
уникальности продолжения, а не предлагаемая починка NVG.

## Воспроизведение и проверка программы

Для этого отдельного расчёта нужен Python с установленным `mpmath`.
Из корня репозитория:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -B verification/nvg_density_universality_audit.py
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=verification python3 -B -m unittest verification.test_nvg_density_universality_audit -v
```

Модуль `source_complete_scaling_saturation_audit.py` используется здесь
только как общая библиотека `BulkModel` и `inverse_potential_jet`.
Его отдельный старый CLI требует `source_complete_solution_audit.py`
и не входит в этот самостоятельный пакет проверки; для воспроизведения
результатов выше достаточно двух приведённых команд.

Программа заново вычисляет пять случаев и 35 строк, а не читает таблицу
готовых результатов; выдаёт JSON и не записывает научные файлы. Пройдены
13 целевых тестов. При независимой приёмке сверены восемь численных строк
и оба коэффициента `ΔZ`, дополнительно заново найдены два корня. Восемь
намеренно испорченных вариантов отчёта отвергнуты; основной вход программы
при повреждённом результате выдаёт `ERROR` и ненулевой код завершения.

В первой версии обнаружилось принятие некоторых подменённых чисел.
Проверка исправлена: численные показатели связаны с исходными уравнениями,
а не только с сохранёнными флагами успеха. Все 35 научных состояний и оба
локальных различителя после исправления остались неизменными. Эти тесты
проверяют расчёт и целостность отчёта, а не подтверждают теорию экспериментом.
