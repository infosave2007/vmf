# Нелокальный статический отклик NVG: конечный импульс

## Краткий результат

В этой работе локальное приближение Томаса–Ферми заменено на конечный
`q`-отклик того же нулетемпературного Dirac/Hartree-функционала. Параметры не
перенастраивались, новые контрчлены не добавлялись. Использована явно
названная схема `SUBTRACTED_MEDIUM_DIRAC_STATIC`: детерминант среды минус
детерминант нулевой плотности при том же флуктуирующем поле.

Главный численный эффект — `chi` уменьшается относительно TF. При принятом
знаке источника это означает более слабый отклик и большую обратную
плотностную жёсткость, а не просто «мягкость». Для физических импульсов и
масштаба `s=1` относительное изменение `(chi_nonlocal/chi_TF-1)` равно:

| фон | `q=100 MeV` | `q=200 MeV` |
|---|---:|---:|
| исходный Q4 при `n0` | −0.147% | −0.646% |
| W8, `y*=0.90` | −0.659% | −2.765% |
| W8, `y*=0.93` | −1.183% | −4.790% |

Для `s=1.3` (с однократным применением градиентного масштаба) соответственно
получены `−0.147%, −0.646%` (Q4), `−0.646%, −2.695%` (W8 `0.90`) и
`−1.172%, −4.685%` (W8 `0.93`). При
заданном `q/kF=2.5` сдвиг существенно больше: примерно `−33.66%` для Q4,
`−64.94%` для W8 `0.90` и `−72.97%` для W8 `0.93` при `s=1`. Эти большие
числа относятся только к выбранной однородной среде и каноническому
градиентному усечению; это не проверенная точность при всех `q` и не
измеренная передаточная функция.

## Что именно вычислено

Для `E=sqrt(p^2+M^2)` и `L=log| (q+2p)/(q-2p) |` производитель вычисляет
радиальные PV-интегралы по `0 ≤ p ≤ kF`

```text
Pi_vv = d/(2*pi^2) ∫ dp p^2/E [1+(E^2-q^2/4)/(p q) L],
Pi_ss = d/(2*pi^2) ∫ dp p^2/E [-1+(M^2+q^2/4)/(p q) L],
Pi_vs = d M/(2*pi^2 q) ∫ dp p L.
```

В точке `q=0` используется термодинамический предел

```text
Pi_vv=N0,  Pi_vs=(M/EF)N0,
Pi_ss=(M/EF)^2 N0 - I,
I=d/(2*pi^2) ∫_0^kF dp p^4/E^3.
```

Последний отрицательный член обязателен: промежуточные отрицательно-
энергетические состояния не являются опциональной «поправкой». Контроль,
оставляющий только particle–hole, даёт другой `Pi_ss` и другой отклик, поэтому
не принимается как альтернативная модель. Логарифмическая особенность при
`p=q/2` интегрируется вычитанием гладкого коэффициента и аналитическим
возвратом его интеграла; искусственная ширина, обрезание и удаление строки не
используются.

Затем выполняется тот же Legendre-мост и седловое исключение `A0`, что и в
пространственном расчёте:

```text
a=1/Pi_vv,
b=M_N Pi_vs/Pi_vv,
d_F=M_N^2(Pi_vs^2/Pi_vv-Pi_ss),
d0=d_F+U_yy-m_omega^2 A0^2,
H=[[a,b,g],[b,d0+Z_s q^2,h],[g,h,-(t0+q^2)]],
Z_s=(s W0)^2.
```

`chi_nonlocal` использует `Pi(q)`. Сравнение `chi_TF` оставляет тот же конечный
`q` в бозонном пропагаторе и градиенте, заменяя только неприводимое
фермионное ядро на его точное `q=0` значение. Поэтому сравнение не смешивает
конечный импульс с полной заменой модели. Здесь источник записан как
`−delta_mu_ext*n`, поэтому `delta n=chi*delta_mu_ext`; точка `q=0` — это
гранд-канонический длинноволновый предел, а точная однородная мода при
фиксированном общем `N` исключена.


## Что удалось свести в одну формулу

В данном статическом секторе термодинамический предел, квантовый отклик
фермионов и скалярно-векторное взаимодействие теперь объединены:

\[
\boxed{\displaystyle
\chi^{-1}(q)=\frac{1}{\Pi_{vv}}+\frac{g^2}{L}
-\frac{\left(M_N\frac{\Pi_{vs}}{\Pi_{vv}}+\frac{gh}{L}\right)^2}
{U_{yy}-m_\omega^2A_0^2+
M_N^2\left(\frac{\Pi_{vs}^2}{\Pi_{vv}}-\Pi_{ss}\right)
+Z_s q^2+\frac{h^2}{L}} }
\]

Здесь все `Pi` вычисляются при том же `q`, `L=m_omega² y²+q²`,
`g=g_omega`, `h=-2 m_omega² y A0`, `Z_s=(s W0)²`.
Формула применима на рассматриваемой устойчивой ветви при невырожденных
знаменателях; неположительная кривизна не превращается в положительный
отклик. При `q=0` она возвращает `chi=1/(d mu/d n)`, при конечном `q`
использует рассчитанные выше нелокальные ядра. Это одна рабочая формула
для указанного сектора модели, **не универсальная теория всей физики**.

Есть и простая точная форма смешанного ядра в принятой схеме:

\[
\Pi_{vs}(q)=\frac{d M k_F}{2\pi^2}F\!\left(\frac{q}{2k_F}\right),\qquad
F(x)=\frac12+\frac{1-x^2}{4x}\log\left|\frac{1+x}{1-x}\right|,
\]

с пределами `F(0)=1` и `F(1)=1/2`. Общая форма связана с известной
кинематикой газа Ферми и функцией Линдхарда, а не является новым
независимым свидетельством в пользу NVG.

## Покрытие и проверки

Рассмотрены исходный Q4-фон при `n0`, а также уже существующие обратные W8
альтернативы `y*=0.90` и `0.93`; для каждой взяты `s=1` и `1.3`. Сетка каждой
пары содержит `q/kF=0, 0.1, 0.5, 1, 1.5, 2, 2.5` и `q=100, 200 MeV`, всего 54
строки. Положительные, неустойчивые и сингулярные статусы не фильтруются.
Расчёт повторяется при 40 и 55 десятичных знаках; объявленная численная
погрешность `1e-9` не является физической неопределённостью. Дополнительно
проверены термодинамический предел `q=0`, непрерывность при `q/kF=10^-4`,
независимая квадратура смешанного ядра и нерелятивистский предел
`M/kF=10^4` при `q/kF=0.5,1,2,2.5` с функцией Линдхарда, включая точку `x=1`.

Все строки при объявленных фонах оказались устойчивыми, но это не является
универсальным утверждением об устойчивости при любых плотностях и импульсах.
Поправка конечного `q` различается между Q4 и W8 и не должна описываться как
универсальное усиление различия: абсолютные отклики могут сближаться или
расходиться в зависимости от фона. Канонический масштабный эффект `s=1`
против `1.3` остаётся в конечном-импульсном пропагаторе: например, при
`q=200 MeV` для Q4 `chi=5638.2195` и `5638.0922 MeV^2`, для W8 `y*=0.90`
`26526.4867` и `24100.6335 MeV^2`, для W8 `y*=0.93` `41675.4356` и
`40261.0570 MeV^2`. При `q=0` этот градиентный масштаб слеп. Следовательно,
прежний вывод о возможной конечной-`q` дискриминации сохраняется как условное
свойство этого класса моделей, но не как извлечённое наблюдение.

## Граница интерпретации

Это реальная статическая неприводимая поляризация среды, согласованная с
принятым no-sea функционалом. Она не является полной перенормированной
вакуумной RPA, динамическим спектром, причинным откликом, расчётом конечного
ядра или экспериментальным подтверждением. Градиентные коэффициенты и `U`
остаются текущим каноническим EFT-усечением; независимые вакуумные
производные, изовекторная/композиционная физика, поверхность и динамика не
определены этим расчётом.

Производитель и контракт: `verification/nvg_nonlocal_response.py` и
`verification/contracts/nonlocal_response.md`.

## Основные источники

- R. J. Furnstahl, J. Piekarewicz, B. D. Serot, *Covariant RPA in Effective Hadronic Field Theory*, [arXiv:nucl-th/0205048](https://arxiv.org/abs/nucl-th/0205048).
- H. Kurasawa, T. Suzuki, *Roles of Antinucleon Degrees of Freedom in the Relativistic Random Phase Approximation*, [PTEP 2015 113D02](https://doi.org/10.1093/ptep/ptv151).
- J. Lindhard, *On the Properties of a Gas of Charged Particles*, [MFM 28 (1954)](https://gymarkiv.sdu.dk/MFM/kdvs/mfm%2020-29/mfm-28-8.pdf).
