# Конечное `N` в сферическом W8 Thomas–Fermi расчёте

Этот документ описывает отдельный условный численный шаг для двух уже
заданных обратных конструкций W8, `y*=0.90` и `y*=0.93`.  Плотность
насыщения `0.16 fm^-3`, энергия связи `-16 MeV` и `K=240 MeV` были входами
обратного построения потенциала.  Поэтому таблица ниже не является
предсказанием, подгонкой по данным или утверждением, что `N=40,208`
соответствуют реальным ядрам.

## Что решается

Программа `verification/nvg_finite_droplet_audit.py` решает сферические
уравнения в переменных

\[
x=W_0r/(\hbar c),\quad a=A/W_0,
\]

\[
\begin{aligned}
y''+2y'/x &= U_y/W_0^4+(M/W_0)n_s/W_0^3-q^2ya^2,\\
a''+2a'/x &= q^2y^2a-g n/W_0^3,\\
Q'&=x^2n/W_0^3,\quad N=4\pi Q(X),
\end{aligned}
\]

где

\[
\nu=\mu-gA,
\quad
n=\begin{cases}
{d\over6\pi^2}(\nu^2-M^2y^2)^{3/2},&\nu>M y,\\
0,&\nu\le M y.
\end{cases}
\]

Здесь `A` вычисляется из не локального уравнения Гаусса.  Локальная
замена на однородное `C_v n²/(2y²)` для неоднородного профиля не используется.
На центре стоят регулярные условия, на внешней границе — `y=1`, `A=0`.
Размеры 16 и 24 fm являются конечными приближениями; большая область нужна
для отдельной проверки хвоста.

Энергия разбита на независимые слагаемые
`T_W, V_U, E_F, T_A, V_A`.  Для поля Гаусса проверяется не только
`S=∫g n A`, но и конечный поток

\[
2(T_A+V_A)=S+4\pi W_0X^2a(X)a'(X).
\]

Терминальная метка стационарности принимает это тождество только при
конечной положительной гауссовой энергии и относительной невязке не более
`10^-6` относительно `2(T_A+V_A)`.  Это отдельный guard, а не нормировка на
полную энергию покоя.

Для фиксированного `N` сжатие `n_λ(r)=λ³n(λr)`, `y_λ(r)=y(λr)` проверяет

\[
D=-T_W-3V_U+3\int P_Fd^3r+T_A+3V_A.
\]

В каждой точке `λ` уравнение Гаусса решается заново; старое поле не
масштабируется как готовый ответ.  Сырая квадратичная норма `N` и её дрейф
показываются отдельно; перед каждым пересчётом Гаусса профиль получает
малую нормировку к целевому `N`, после чего контролируемый дрейф также
проверяется (предел относительного контролируемого дрейфа `10^-10`; сырая
ошибка и величина коррекции остаются в JSON).

## Полученный результат

Ниже приведены терминальные 24-fm проверки.  Знак `E/N-M` — это только
энергетический критерий этой модели.

| W8 | N | μ (MeV) | E/N−M (MeV) | rms (fm) | интерпретация |
|---|---:|---:|---:|---:|---|
| 0.90 | 40 | 939.6922 | +0.4464 | 17.8189 | сходящаяся, но газовая/нелокализованная ветвь |
| 0.90 | 208 | 931.2971 | −3.9110 | 5.8283 | численный связанный стационарный кандидат |
| 0.93 | 40 | 939.9591 | +0.5773 | 18.5632 | сходящаяся, но газовая/нелокализованная ветвь |
| 0.93 | 208 | 932.5268 | −1.7355 | 4.9129 | численный связанный стационарный кандидат |

Для двух `N=208` кандидатов независимые относительные невязки поля после
уточнения области составили примерно `5.4·10^-5` и `6.1·10^-5`, относительная
невязка `N` — `2.7·10^-12` и `5.1·10^-9`, а `|D|/E` — `2.1·10^-9` и
`3.6·10^-10`.  Для терминальной метки требуются все три уровня уточнения,
обе соседние разности и явное сравнение fine-16 fm с доменом 24 fm.  Три
уровня сетки/допуска и 24-fm пересчёт дают изменение
`E/N` существенно меньше `0.02 MeV`, а rms — меньше `0.02 fm`.  Независимый
конечный-difference oracle с повторным решением Гаусса согласуется с
аналитическим `D` после Richardson-экстраполяции на масштабе менее `0.1 MeV`.

Ветки `N=40` имеют `E/N>M` и заметную долю числа частиц во внешних 20%
области; они не являются локализованными каплями и пропускают некоторые
численные пределы протокола, но это не теорема об отсутствии связывания.
Некоторые из трёх
детерминированных начальных радиусов дополнительно заканчиваются
`FAILED_NUMERICAL_PROTOCOL`; это сохранено в JSON и не превращено в
физический запрет.  Сходящаяся связанная ветвь — лишь стационарный кандидат
этой ограниченной TF-модели, а не доказательство полной устойчивости:
положительная кривизна одного масштабного направления не заменяет спектр
возмущений.

## Воспроизводимость и границы

```text
PYTHONDONTWRITEBYTECODE=1 python3 -B verification/nvg_finite_droplet_audit.py
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=verification python3 -B -m unittest verification.test_nvg_finite_droplet_audit -v
```

Команда печатает свежий строгий JSON и ничего не записывает.  Все значения
получаются из запуска; сохранённая таблица результатов не используется как
вход.  Расчёт не включает кулоновское поле, асимметрию и `rho`, спаривание,
оболочечные эффекты, spin–orbit, квантовый RPA, crust, beta-равновесие,
гравитацию или эмпирический вес.  Поэтому он не закрывает задачу реальных
конечных ядер и не подтверждает NVG как универсальную теорию.
