# Конечная температура: единый отклик NVG (6 компонент трёх операторов)

Этот файл описывает **P1/A1**, а не новую силу и не завершение всей теории. В
репозитории добавлен живой расчёт `verification/nvg_thermal_master_response.py`.
Он заново строит восемь локальных термальных ветвей W8/U16 (`T=70 MeV`,
`μ=775,875 MeV`, якоря `y*=0.90,0.93`), не читая JSON-таблицу как вход. Для
каждого состояния, `q=100,200 MeV`, двух неизменённых масштабов `\lambda=1,1.3` и
пяти проб `z=0, iT, i2πT, i4πT, (0.6+0.2i)q` сохраняются все `160` строк.
Отдельно вычисляются `32` строки аналитического пространственного разреза.

## Формула и единая конвенция

Положим

\[
 E_p=\sqrt{p^2+m^2},\qquad
 E_k=\sqrt{p^2+q^2+2pqu+m^2},\qquad p_z=pu,\quad k_z=pu+q,
\]

и

\[
 \delta N_s(p)=s f((E_p-s\nu)/T),\qquad
 f(x)=\frac1{e^x+1},\qquad s=\pm1.
\]

Вакуумно-вычтенный medium-ответ строится непосредственно из полного набора
положительных и отрицательных проекторов:

\[
 K_{AB}(q,z)=\frac{d}{4\pi^2}\int_0^\infty p^2dp\int_{-1}^{1}du
 \sum_{s,t=\pm1}
 \frac{\delta N_s(p)-\delta N_t(k)}{tE_k-sE_p-z}\,V_{AB}^{st}.
\]

Здесь `d=4`, а используемые вершины (порядок каналов `vv,vs,ss,vL,sL,LL`)
равны

\[
\begin{aligned}
V_{vv}&=\tfrac12[1+st(\mathbf p\!\cdot\!\mathbf k+m^2)/(E_pE_k)],\\
V_{vs}&=\tfrac m2(s/E_p+t/E_k),\\
V_{ss}&=\tfrac12[1+st(m^2-\mathbf p\!\cdot\!\mathbf k)/(E_pE_k)],\\
V_{vL}&=\tfrac12(sp_z/E_p+tk_z/E_k),\\
V_{sL}&=\frac{stm(p_z+k_z)}{2E_pE_k},\\
V_{LL}&=\tfrac12[1+st(2p_zk_z-\mathbf p\!\cdot\!\mathbf k-m^2)/(E_pE_k)].
\end{aligned}
\]

Все шесть интегралов выполняются до проверки тождеств, а не восстанавливаются
из них постфактум. На конечной сетке проверяется

\[
 qK_{vL}=zK_{vv},\qquad qK_{sL}=zK_{vs},\qquad qK_{LL}=zK_{vL}.
\]

При `z=0` для вырожденного `s=t` применяется точная разделённая разность
`f((E-sν)/T)(1-f((E-sν)/T))/T`: знак полосы сокращается с
знаком знаменателя, поэтому предел положителен для обеих полос. Это устраняет
ложное деление `0/0`. Сетка по `p` имеет только
фиксированные численные разбиения около термального края и `q/2`; предел
интегрирования `5000 MeV` — численная аппроксимация хвоста, не новый физический
УФ-срез. Независимая проверка использует `6000 MeV` и более тонкие узлы.

## Полный продольный седловой отклик

Для каждого `Kvv,Kvs,Kss` сохраняется тот же Hartree/subtracted-medium
конструкт, без второго применения масштаба:

\[
 a=K_{vv}^{-1},\quad b=M_NK_{vs}/K_{vv},\quad
 d_F=M_N^2(K_{vs}^2/K_{vv}-K_{ss}),
\]

\[
 d_0=d_F+U_{yy}-m_\omega^2A_0^2,\quad
 h=-2m_\omega^2yA_0,\quad t_0=m_\omega^2y^2,
\quad Z=(\lambda W_0)^2,\qquad \lambda\in\{1,1.3\}.
\]

\[
H=\begin{pmatrix}
 a&b&g&-gz/q\\
 b&d_0+Z(q^2-z^2)&h&0\\
 g&h&-(q^2+t_0)&zq\\
 -gz/q&0&zq&t_0-z^2
\end{pmatrix}.
\]

Здесь `\lambda` — коррелированный scale-фактор, применённый ровно один раз;
`s,t` в формуле выше остаются метками энергетических полос.

Плотностной ответ получается двумя способами:

\[
 \chi=(H^{-1})_{nn}=\frac1{D-B^2/C},
\]

где

\[
\begin{aligned}
L&=t_0+q^2-z^2,\\
D&=a+g^2(1-z^2/q^2)/L,\\
B&=b+gh/L,\\
C&=d_0+Z(q^2-z^2)+h^2(1-z^2/t_0)/L.
\end{aligned}
\]

Каждая строка содержит исходную матрицу `H`, прямое решение `4×4`, величины
Шура и их относительную разницу. Отрицательная необработанная временная
векторная седловая кривизна не объявляется физической неустойчивостью: она
устраняется согласованным гауссовым/шуровским исключением.

## Пределы и контрольные утверждения

* `q=0,z=0` — однородный термодинамический grand-canonical предел. При
  `q=0,z≠0` плотностной и смешанный каналы зануляются сохранением числа, но
  скалярный interband/pair-канал и `LL`-компонента не зануляются. Независимая
  угловая интеграция даёт `Π_LL=-4.97300044104 MeV²` при
  `m=500,ν=0,T=70,z=i70`; это не утверждение об устойчивости. Для проверки
  использована независимая формула
  `Π_LL=-2d/π² ∫dp p² E(f_++f_-)(1-p²/(3E²))/(4E²-z²)` и отдельная
  двумерная угловая сумма проекторов.
* На мнимой оси `z=iξ` получены аналитические пробы. Положительность нескольких
  `χ(iξ)` записана только как диагностическая проверка; конечная сетка не есть
  сумма Мацубары, не есть равновременная квантовая ковариация и не доказывает
  глобальную спектральную положительность или причинность.
* Полный проекторный набор даёт малую сырую Ward-ошибку и совпадает в `z=0` с
  поддерживаемым термальным пространственным производителем. Контроль только
  particle-hole ветви намеренно нарушает Ward и отклоняется.
* Проверены чётность по `ν` (vv/ss — чётные, vs/sL — нечётные), сопряжение по
  частоте, оба масштаба, прямое `4×4` решение и сходимость тонкой квадратуры.
  Для `q→0` при конечном `z` проверены нулевые `vv/vs`, сохранённые `ss/LL` и
  независимые 1D/2D формулы; контроль малых `q=10^{-3}` имеет абсолютную
  плотностную ошибку меньше `1.6×10^{-7}`.
  Для `z\mapsto-z^*` каналы `vL,sL` меняют знак, остальные — сопрягаются без
  этого знака.

## Аналитическое поглощение в пространственном разрезе

Для `0<ω<q` отдельный API `thermal_cut_imag` вычисляет **голую** мнимую часть
без `η`:

\[
 E_{\min}=\frac{q\sqrt{1+4m^2/(q^2-ω^2)}-ω}{2},\qquad
 \Delta f_s(E)=f\!\left(\frac{E-sν}{T}\right)-f\!\left(\frac{E+ω-sν}{T}\right),
\]

\[
\begin{aligned}
\operatorname{Im}\Pi_{vv}&=\frac d{4\pi q}\int_{E_{\min}}^\infty dE\,(\Delta f_++\Delta f_-)
 [E(E+ω)+(ω^2-q^2)/4],\\
\operatorname{Im}\Pi_{vs}&=\frac d{4\pi q}\int_{E_{\min}}^\infty dE\,(\Delta f_+-\Delta f_-)m(E+ω/2),\\
\operatorname{Im}\Pi_{ss}&=\frac d{4\pi q}\int_{E_{\min}}^\infty dE\,(\Delta f_++\Delta f_-)
 [m^2+(q^2-ω^2)/4].
\end{aligned}
\]

Тёплые (не `T=0`) хвосты дают ненулевое поглощение во всём объявленном окне
`0<ω<q`, хотя оно может быть экспоненциально мало. В фиксированном
контроле `m=700,ν=760,q=100,ω=75 MeV` холодная аналитическая формула даёт
ровно ноль, а при `T=70 MeV` интеграл положителен. При `T=0.1 MeV`
численный API честно сообщает underflow по всей доступной области, а не выдаёт
физический ноль. Если `E_min` находится внутри заполненного моря, проверка
термального края переносится к `E\simeq|ν|-ω/2`; разбиение интеграла сохраняет
точки `|ν|-ω` и `|ν|`. Например, при `m=700,ν=1200,q=100,ω=25,T=0.1 MeV`
прямой разрез совпадает со старой `analytic_ph_imag` с относительной ошибкой
менее `3×10^{-6}`, а отражение `ν\mapsto-ν` сохраняет vv/ss и меняет знак vs.
Независимая свёртка старой холодной `ImΠ` по производной Ферми при
`m=700,ν=760,q=100,ω=25,T=2 MeV` совпадает с прямым тёплым разрезом с
относительной ошибкой менее `5×10^{-11}`; свёртка относится только к голой `Π`.
Это не ширина
взаимодействующего полюса, не collision rate и не предсказание HADES. Нельзя
переносить холодный критерий «полюс вне края не затухает» на тёплую среду.

## Фактический численный мост и воспроизведение

Ниже приведены контрольные строки из финального JSON (MeV-натуральные единицы):

| состояние | q | z | `Pi_vv` | `Pi_ss` | `chi` (\lambda=1) |
|---|---:|---|---:|---:|---:|
| W8, y*=0.90, μ=775 | 100 | 0 | 10468.49 | 8111.83 | 10518.8678+0i |
| W8, y*=0.90, μ=875 | 100 | iT | 3544.15 | 1915.92 | 3115.7481−3.94×10⁻¹²i |
| W8, y*=0.90, μ=875 | 200 | (0.6+0.2i)q | −2858.22+6934.60i | −2731.89+4615.83i | 7062.2339−1950.6719i |

Таблица не является подгонкой и не заменяет 160 строк; полные значения и обе
шкалы находятся в `verification/nvg_thermal_master_response_results.json`.
Команды воспроизведения без кэша результата:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest verification/test_thermal_master_response.py
PYTHONDONTWRITEBYTECODE=1 python3 -B verification/nvg_thermal_master_response.py \
  --output verification/nvg_thermal_master_response_results.json
```

Финальные проверки: `160` response + `32` cut rows, max raw-Ward relative
error `1.31×10^-9`, max direct-vs-Schur `5.14×10^-16`; интегральные ошибки
и хвостовые underflow-события сохраняются в строках/контролях.

## Статус и граница применения

Результат — воспроизводимый расчёт линейного finite-T/static/dynamic bridge
для **выбранного существующего действия**. Он не утверждает завершение
вакуумной ренормировки, полного RPA, положительной спектральной меры,
глобальной устойчивости, состава ядерного вещества, конечного ядра, кластеров,
детекторной карты или эмпирического согласия. Равенство ФДТ, полный хвост
Мацубары и observable-map для эксперимента здесь не замыкаются. Квантовая
конечная система,
состав, полный спектр, транспорт и гравитационный сектор остаются открытыми.
Новые силы, скрытая дисперсия, подгонка к отложенному остатку, физическая `η`
и заявления о цикле энергии/BH в этой работе не вводятся.

В качестве первичных ориентиров по сохраняющемуся RPA и отрицательно-энергетическим
промежуточным состояниям используются [nucl-th/0106061](https://arxiv.org/abs/nucl-th/0106061)
и [nucl-th/0205048](https://arxiv.org/abs/nucl-th/0205048). Для различия
термального отклика и квантовой дисперсии см. раздел II
[arXiv:1509.06741](https://arxiv.org/html/1509.06741v2). Эти источники
мотивируют известную физическую структуру; конкретная 160+32 строковая
квадратура и её проверки являются реализацией проекта, а не новым законом.
