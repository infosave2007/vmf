# Тепловой composition-Hessian: локальный мост состава (P2-C)

## Что рассчитано

Новый producer `verification/nvg_thermal_composition_hessian.py` заново
строит принятые калибровки W8/U16 и интегралы конечной температуры. Для
двух спин-вырожденных компонент (`d=2` для каждой, частица и античастица)
решается один и тот же локальный положительный корень для заданных
`B=n_n+n_p` и `D=n_n-n_p`:

\[
 f_T=\sum_{i=n,p}(\nu_i n_i-P_i)+U(y)+{C_vB^2\over2y^2}
       +{C_\rho D^2\over2}.
\]

Температура фиксирована `T=70 MeV`. Восемь опорных плотностей получены
**живым** grand-canonical решением при якорях `y*=0.90,0.93`, моделях W8/U16
и `mu_reference=775,875 MeV`; сохранённая таблица результата не является
входом расчёта. Для каждого из 16 сочетаний
`(anchor, mu_reference, model, delta=0,0.2)`
сохранены обе конвенции контакта: `C_rho=0` и фиксированный исторический
`j=11.13601607117819 MeV`,

\[
 C_\rho={2j\over n_0}=139.200200889727375\ {\mathrm{MeV\,fm^3}},\qquad
 n_0=0.16\ {\mathrm{fm^{-3}}}.
\]

Итого публичный producer содержит 32 строки. В естественных единицах
`C_rho=1.8116756685\,10^{-5} MeV^{-2}`; переход к публикуемым кривизнам
выполнен умножением на `(hbar c)^3`, `hbar c=197.3269804 MeV fm`.
Здесь `j` и `C_rho` — заранее известные условные входы; их совпадение с
историческим переносом не является новой силой или независимой подгонкой.

## Аналитическая кривизна и проверки

После исключения скалярного поля используется трёхпеременная
Legendre/Schur-формула. При фиксированных `n_n,n_p`

\[
 C=U_{yy}+M_N^2\sum_i\left(S_{m,i}+{S_{\nu,i}^2\over N_{\nu,i}}\right)
       +{3C_vB^2\over y^4},\quad
 b_i={M_NS_{\nu,i}\over N_{\nu,i}}-{2C_vB\over y^3},
\]

\[
 H_{\rm species}=\operatorname{diag}(1/N_{\nu,i})+{C_v\over y^2}{\bf1}{\bf1}^T
 +C_\rho vv^T-{bb^T\over C},\quad v=(1,-1),
\]

\[
 H_{BD}=R^TH_{\rm species}R,\qquad
 R={1\over2}\begin{pmatrix}1&1\\1&-1\end{pmatrix}.
\]

`mu_B=(mu_n+mu_p)/2` и `mu_D=(mu_n-mu_p)/2` вычисляются в каждом
каноническом составе, а не заменяются опорным `mu_reference`. Для каждого
из 32 рядов независимые симметричные конечные разности `dmu_B,dmu_D`
проверяются при `h/B=10^-3,5\,10^-4,2.5\,10^-4`. Дополнительно выполнены:

* симметрия Максвелла, положительность `C`, собственных значений и
  `det(H)` на локальной ветви;
* при `delta=0` и `C_rho=0` — связь `H_BB` с принятой тепловой
  grand-canonical `q=0` кривизной `f_nn`;
* симметричный изовекторный предел:
  \[
  \Pi_{vv}=N_{\nu,n}+N_{\nu,p},\qquad
  \chi_{DD}={\Pi_{vv}\over1+C_\rho\Pi_{vv}}.
  \]
  Здесь `Pi_vv` имеет естественные единицы `MeV^2`, `C_rho` — `MeV^-2`,
  а `chi_DD` после умножения/инверсии в опубликованных единицах имеет
  `fm^-3 MeV^-1`; формула условна для фиксированного известного `j`.
* обмен компонент `delta -> -delta` (ограниченный контроль);
* контактная идентичность: корень не меняется, `mu_D` сдвигается на
  `C_rho D`, а меняется только `H_DD` на `C_rho`, свободная энергия и
  давление на `C_rho D^2/2`;
* полный термодинамический экспорт:
  \[
  P=P_{\rm kin}-U+{C_vB^2\over2y^2}+{C_\rho D^2\over2}
   =\mu_BB+\mu_DD-f_T.
  \]
  Производная свободной энергии по `B,D` проверяется независимой
  симметричной разностью на тех же shifted roots (envelope identity).
* термодинамический переход к переменным `(B,Q=n_p)`:
  `H_BQ=S^T H_BD S`, `S=[[1,0],[1,-2]]`, без интерпретации как
  экспериментальной charge/acceptance projection;
* независимый контроль порядка квадратуры/точности и явное сохранение
  неудачных корней, вместо принудительного выбора благоприятной ветви.

## Численный результат

Фактические числа, количество строк, невязки и fingerprints находятся в
`verification/nvg_thermal_composition_hessian_results.json`; они
перегенерируются командой:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -B verification/nvg_thermal_composition_hessian.py \
  --output verification/nvg_thermal_composition_hessian_results.json
```

Фокусированный тест — `verification/test_thermal_composition_hessian.py`.
Проверенный `status` producer — `PASS_LIVE_THERMAL_COMPOSITION_HESSIAN`.
Численная положительность в этих 32 локальных строках означает лишь, что
данная ограниченная однородная ветвь допускает локальную термодинамическую
кривизну. Это не доказательство глобального минимума, фазового равновесия,
причинности или полноты EOS.

Небольшой контрольный срез результата (все числа получены живым запуском):

| модель/состав | `B` (fm⁻³) | `H_BB` | `H_BD` | `H_DD` | `det H` |
|---|---:|---:|---:|---:|---:|
| W8, `delta=0`, `C_rho=0` | 0.1021793165 | 606.4623992 | 0 | 724.7610862 | 4.395403472×10⁵ |
| W8, `delta=0`, fixed `j` | 0.1021793165 | 606.4623992 | 0 | 863.9612871 | 5.239600350×10⁵ |
| U16, `delta=0`, `C_rho=0` | 0.1021797530 | 606.4690375 | 0 | 724.7581865 | 4.395433998×10⁵ |
| W8, `delta=0.2`, `C_rho=0` | 0.1021793165 | 634.9608049 | −142.3382259 | 753.3050013 | 4.580589794×10⁵ |

По всем 32 строкам максимальные диагностические величины: корневая
невязка `3.65×10⁻⁴⁴`, ошибка pressure identity `1.67×10⁻⁴³`, ошибка
конечной разности Hessian `1.10×10⁻⁶`, независимая конечная-разностная
ошибка Максвелла `3.14×10⁻⁹`, envelope-ошибка `3.38×10⁻⁷`, ошибка
контрольной квадратуры `2.10×10⁻²⁰`.
Фиксированный контакт добавляет в `H_DD` ровно `139.200200889727375`
`MeV fm³` (например, 724.7610862 → 863.9612871), не меняя корень или
`H_BB`.

Например, воспроизведение pressure/envelope identity и всех строк выполняется
той же командой CLI выше; отдельная быстрая проверка сохранённого результата:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -B - <<'PY'
import json, sys
from pathlib import Path
sys.path.insert(0, "verification")
import nvg_thermal_composition_hessian as c
r = json.loads(Path("verification/nvg_thermal_composition_hessian_results.json").read_text())
print(c.validate_result(r), r["controls"]["pressure_identity"]["all_pass"])
PY
```

Корневая величина порядка `10^-44` — это запас арифметической точности
решателя и квадратуры, а не неопределённость физических входов, модели или
исторического коэффициента.

## Границы вывода

Контакт `j` — условный перенос уже принятого исторического коэффициента, а
не новая сила и не универсальная эмпирическая проверка. Здесь нет электронов,
бета-равновесия, Coulomb/кластеров, конечного ядра, изоэнтропической
скорости звука, TOV, детекторной карты или сравнения с HADES. Открытыми
остаются глобальная фазовая структура, полный charge-сектор, quantum-shell
и gravity bridges. `evidence_weight=0.0`: вычисление является проверяемым
математическим мостом, не независимым экспериментальным подтверждением.
